# Port for the server to run on
PORT=8080

# Your Todoist application's Client ID
TODOIST_CLIENT_ID="your_todoist_client_id_here"

# Your Todoist application's Client Secret
TODOIST_CLIENT_SECRET="your_todoist_client_secret_here"

# The public base URL of this application (e.g., http://localhost:8080 or https://your-timetodo-app.com)
# Used for constructing redirect URIs for OAuth and for webhook registration if applicable.
APP_BASE_URL="http://localhost:8080"

# The Pebble timeline API endpoint
PEBBLE_TIMELINE_API_URL="https://timeline-sync.rebble.io"

# A secret key used for HMAC verification of webhooks and potentially session management.
# Generate a strong random string for this value.
SECRET_KEY="openssl rand -base64 32"

# Google Cloud Project ID
GCP_PROJECT_ID="your_gcp_project_id_here"

# Google Service Account. Since this runs on Google Cloud, this uses magic and fairy dust by default. Otherwise, uncomment this next key and paste in the path to your service account JSON key file.
# This is purely handled by the Google Cloud SDK and is not required if running on GCP. Read Google's docs to learn more: https://github.com/googleapis/google-cloud-go?tab=readme-ov-file#authentication
# Do note: if you're developing locally, you probably don't need to set this: read the docs above. TL;DR: use the gcloud CLI to authenticate your local environment.
# GOOGLE_APPLICATION_CREDENTIALS=""

# OpenTelemetry Exporter Endpoint. Self-hosted or third-party endpoint for OpenTelemetry data. Must be able to be reached by the application for obvious reasons.
OTEL_EXPORTER_ENDPOINT=""

